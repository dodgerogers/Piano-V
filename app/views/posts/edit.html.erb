<h1>Edit song</h1>
	<% if !current_user %><br/>
		<p>please login to edit</p><br/>
		<% else %>
			<% if current_user.id != @post.user.id %>
				You can only edit your own songs<br/>
			<% else %>
				<%= render 'form' %>
			<% end %>
	<% end %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
